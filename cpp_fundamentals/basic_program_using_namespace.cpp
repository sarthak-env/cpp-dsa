#include<iostream>
using namespace std; //this line allows us to use names from the std namespace without prefixing them with std::

int main()
{
    cout<<"This is how we can use namespace in C++"; // prints the message to the console
    return 0;
}